# Setup

Traefik has additional external network access to ix-dockge_default so that dockge could be accessed through it.

## UI Access

Once Authentik is set up - add support for OIDC login through it.
Top plugin used: https://plugins.traefik.io/plugins/66b63d12d29fd1c421b503f5/oidc-authentication