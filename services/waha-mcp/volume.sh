#!/bin/sh
# Every service in this homelab has one of these, so here it is: waha-mcp is
# stateless and needs no host volumes. It holds nothing; WAHA holds the session.
echo "waha-mcp is stateless; no host volumes needed"
